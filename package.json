{
  "name": "hacksincodigos",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Sitio web de HacksinCodigos — desarrollo web, agentes IA y ciberseguridad en Costa Rica.",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "lint": "astro check",
    "test": "node --test \"tests/**/*.test.mjs\"",
    "postbuild": "node scripts/verify-build.mjs"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "astro": "^5.14.1",
    "@astrojs/sitemap": "^3.6.0",
    "sharp": "^0.34.4"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "typescript": "^5.9.3"
  }
}
